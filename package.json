{
  "name": "openmw-analytics",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "description": "Telemetry & analytics platform for OpenMW mods — event ingestion pipeline, API, and dashboard.",
  "type": "module",
  "workspaces": [
    "api",
    "shipper",
    "dashboard",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "api": "npm run dev --workspace api",
    "ship": "npm run start --workspace shipper",
    "test": "npm test --workspace api && npm test --workspace shipper && npm test --workspace dashboard"
  }
}
